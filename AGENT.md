你是本项目的前端开发工程师。

请根据作业要求开发 SmartCinema 智能影院选座系统。

技术约束：
1. 只使用 HTML5、CSS3、原生 JavaScript。
2. 必须使用 Canvas 绘制电影院座位布局和热度地图。
3. 不允许使用 ECharts、D3、AntV 等第三方 Chart 库。
4. 不依赖后端数据库，数据使用 LocalStorage 或本地 JSON。
5. 项目最终必须可以直接在浏览器打开运行，优先整理为 index.html。
6. 支持 PC、iPad、手机端响应式布局。

核心功能：
1. 登录注册：
   - 普通用户注册后获得会员资格。
   - 用户登录后才能选座、预订、购票。
   - 管理员登录后可以进入后台管理。

2. 影院座位图：
   - 三个放映厅：小厅 100 座，中厅 200 座，大厅 300 座。
   - 共 10 排。
   - 小厅每排 10 座，中厅每排 20 座，大厅每排 30 座。
   - 每个座位显示排号和座位号。
   - 空座绿色，选中未售座位黄色，已售座位红色。
   - 座位必须呈弧形排列，使用 Canvas 绘制。

3. 智能推荐选座：
   - 支持个人票、情侣票、家庭票、团体票。
   - 个人票输入姓名、年龄。
   - 团体票输入人数、成员姓名和年龄。
   - 少年：15 岁以下不能坐前三排。
   - 老年人：60 岁以上不能坐最后三排。
   - 情侣：优先推荐中间区域连续双座。
   - 家庭：优先推荐中后排连续座位。
   - 团体票：5-20 人，必须同排连续，不能分开坐。
   - 团体中如有少年或老年人，也要遵守年龄规则。
   - 推荐结果需要给出推荐理由。

4. 手动选座：
   - 支持单选。
   - 支持 Ctrl + 鼠标点击多选。
   - 支持推荐座位后手动修改。
   - 尽量实现拖拽选座作为加分项。

5. 影院热度地图：
   - 使用 Canvas 绘制热度分布。
   - 红色表示热门区域，黄色表示一般区域，蓝色表示冷门区域。
   - 支持动态显示一周内热度区域变化。

6. 观影体验评分：
   - 根据视角、距离银幕远近、周围空位情况计算系统评分。
   - 输出：极佳、优秀、一般。
   - 支持观众观影后手动评分。
   - 将系统评分和用户评分综合展示。

7. 无障碍模式：
   - 大字体模式。
   - 高对比度模式。
   - 色盲友好模式。
   - 可选语音提示。

8. 订单中心：
   - 支持预订、取消预订、购票、退票。
   - 订单数据保存到 LocalStorage。

9. 加分项尽量实现：
   - AI 观影问答式顾问，可根据用户输入推荐座位并解释理由。
   - 拖拽选座。
   - 动画交互优化。
   - 多人在线模拟或 WebSocket 实时座位更新，如果无法真正实现 WebSocket，可先实现本地模拟。
   - 个性化影院布局或科技感界面。

开发原则：
1. 每次开始修改前，先检查当前目录结构和已有代码。
2. 优先复用已有实现，不要无故重构。
3. 每次只完成我当前要求的阶段，不提前做太多无关功能。
4. 每个阶段完成后，保证项目可运行。
5. 修改后说明：
   - 实现了什么
   - 修改了哪些文件
   - 如何运行
   - 如何测试
   - 还有哪些未完成内容
6. 不要只写伪代码，必须给出可运行实现。
7. 界面要有科技感，操作流程尽量控制在 3 步以内。

现在进入第二阶段开发。

当前第一阶段已经完成：
- 项目基础结构
- index.html / style.css / app.js
- 登录注册界面
- 三个影厅切换
- Canvas 弧形座位图绘制
- 座位状态显示
- LocalStorage 初始化数据

请先检查当前项目目录和已有代码，必须优先复用现有实现。
不要重写整个项目，不要无故重构，不要破坏第一阶段已经能运行的功能。



完成后请告诉我如何在浏览器中运行。