# SmartCinema 用户使用手册

## 1. 使用前说明

SmartCinema 是本地运行的智能影院选座系统。所有账号、座位和订单数据保存在当前浏览器的 LocalStorage 中；换浏览器、清除站点数据或使用无痕窗口时，数据可能不同。

推荐使用 Chrome、Edge、Firefox 或 Safari 的最新版本。可以直接打开 [index.html](index.html)，也可以参考 [README](README.md) 通过本地静态服务运行。

## 2. 账号与入口

### 普通用户

普通用户可使用“注册”创建账号。注册时需要填写用户名、昵称和密码，系统会自动登录并进入选座端。

普通用户可使用：智能推荐、手动选座、体验评分、无障碍设置、预订/购票和个人订单。

### 管理员

管理员测试账号：`admin`。

管理员测试密码：`Admin@123`。

管理员登录后会直接进入后台，不能通过注册页面创建管理员账号。管理员可管理座位、全部订单和普通用户信息。

登录首页提供“无障碍模式”下拉菜单，可在登录前开启大字体、高对比度、色盲友好或语音提示。普通用户在手机端选座页顶部也可随时打开同一菜单，并通过“退出”返回登录页。

### 多标签页登录说明

每个浏览器标签页都有独立登录状态。可以在一个标签页登录管理员，在另一个标签页登录普通用户或其他普通用户，彼此不会顶号；退出其中一个标签页也不会让其他标签页退出。

## 3. 普通用户操作

### 3.1 智能推荐与手动选座

桌面端可直接在“选座信息”中填写票型和同行人资料后生成推荐。手机端以座位图为主：先选择影厅，再直接手动点击座位；需要填写同行人资料或获取推荐时，先点击“选座信息”打开底部抽屉，再按与 PC 端相同的方式填写票型、姓名和年龄。手机端确认座位、预订或购票前都必须填写完整的选座信息；未填写时“确认座位”会打开信息抽屉并提示补充，不会进入订单确认。

生成推荐后，系统会在同一张 Canvas 座位图中自动选中并标记推荐座位，同时给出推荐理由。抽屉会自动收起，用户可继续点击推荐座位取消，或点击其他空座进行增删和替换。

手机端的固定底栏会显示已选座位数量；点击“确认座位”后可查看影厅、座位、票型和人数，再选择预订或购票。

票型说明：

| 票型 | 输入与规则 |
|---|---|
| 个人票 | 输入 1 位观众姓名和年龄。 |
| 情侣票 | 输入 2 位观众信息，系统优先寻找中间区域连续双座。 |
| 家庭票 | 输入家庭成员信息，系统优先寻找中后排连续座位。 |
| 团体票 | 输入 5-20 人成员信息，必须安排在同一排连续座位。 |

年龄限制仅用于智能推荐：15 岁以下观众不推荐前三排；60 岁及以上观众不推荐最后三排。手动选座、预订与购票不会因年龄限制被拦截。

### 3.2 手动选座

<!-- 组员2负责模块：手动选座/拖拽选座。对应 app.js 的 handleCanvasClick()、
     handleCanvasPointerDown/Move/Up/Leave()、applyDragSelection()、getCanvasPoint()。
     这一段是用户视角的操作说明，配合代码看能快速理解交互意图。 -->

座椅采用圆角方形显示，弧形排布、排号和座位号保持不变。

- 单击空座：选择一个座位。
- 手动选座不受年龄推荐限制影响，可按实际需要调整座位。
- 再次单击已选座位：取消选择。
- 桌面端按住 `Ctrl` 后单击：多选或取消某个座位；手机端连续点击空座即可多选，再次点击已选座位即可取消。
- 在座位图空白处按下并拖动：框选矩形范围内的可用座位。
- 已售、已预订和维修/禁用座位不可选择。

颜色和外圈说明：

| 标识 | 含义 |
|---|---|
| 绿色填充 | 空座，可选择。 |
| 黄色填充 | 当前选中的未售座位（包含推荐后直接选中的座位）。 |
| 红色填充 | 已售或已购票座位。 |
| 紫色填充 | 已预订座位。 |
| 灰蓝色填充 | 维修/禁用座位。 |
| 青色外圈 | 系统推荐座位。 |
| 金色外圈 | 推荐后手动调整的座位。 |

### 3.3 热度边框

<!-- 组员2负责模块：热度地图。对应 app.js 的 getHeatSourceSeats()、
     getHeatInfluenceByDistance()、calculateSeatHeat()、getHeatBorderColor()，
     以及 drawSeats() 里画外圈的那一层。 -->

座位内部颜色始终代表座位状态，外圈颜色代表热度，不会改变能否选座。

- 蓝色：冷门。
- 橙色：一般热度。
- 红色：热门。
- 紫色：超热门。

热度由已预订、已购票和已售座位向周围座位扩散。多个热源集中时才会逐步升高；可通过“显示热度边框”开关隐藏或显示外圈。

购买座位时，订单会记录购票日期。选座图上方的“显示热度·变化”会弹出当前影厅截至今天的最近 7 天热度窗口，不显示未来日期，并显示每天由距离扩散结果汇总的区域均热。每天会稳定随机生成空座、已售和预订状态，真实订单会覆盖对应日期的模拟状态；座椅填充和热度外圈使用同一份状态，因此已售座位一定会作为热源参与计算。点击任意日期后，窗口会自动收起并定位到下方现有 Canvas，直接预览对应热度边框；关闭窗口即可回到实时热度显示。

### 3.4 多人实时选座模拟

页面上的“多人实时同步已连接”表示本地实时协作已启用。打开同一项目的两个标签页后：

1. 分别登录管理员和普通用户，或登录两个不同普通用户。
2. 任意一方预订、购票、退票，或管理员修改座位状态。
3. 另一方的座位图、订单和热度边框会自动更新，无需刷新页面。

两名用户可以暂时选中同一空座，但在预订或购票时系统会以最新共享状态再次校验。先提交的用户获得座位；后提交的用户会收到“座位刚刚被其他用户锁定”的提示，并需要重新选择。

> 这是不依赖后端的“多人在线模拟”：系统使用浏览器 `BroadcastChannel` 和 LocalStorage 同步同一浏览器配置文件下的多个标签页。不同设备、不同浏览器或无痕窗口之间不能实时互通；若要实现跨设备的真实 WebSocket，需要额外后端服务。

### 3.5 观影体验评分

选择座位后，系统会自动显示座位参考分、评分等级和评分理由。评分考虑与银幕距离、横向视角、周围空位和年龄/票型规则；选座页不显示观众评分或综合结果。

完成购票后，进入“我的订单”，在对应订单的“评分”中选择 1–5 星。系统会基于该订单的座位参考分与观众评分显示订单专属综合评分。

### 3.6 无障碍模式

在“无障碍模式”中按需开启以下开关：

无障碍设置会在当前登录期间保留；点击“退出登录”后，系统会关闭全部无障碍模式并清除对应配置。再次登录时默认关闭。

- 大字体模式：放大正文、表单和功能区文字。
- 高对比度模式：增强文字、边框和区块对比。
- 色盲友好模式：使用更容易区分的座位颜色。
- 语音提示：选座、推荐成功、预订/购票成功时使用浏览器语音播报。

所有开关会立即生效并在刷新页面后保留。

### 3.7 预订、购票与订单

完成选座后，在订单中心选择“预订当前座位”或“购票当前座位”。系统会检查观众人数、票型、团体连座要求和座位可用性；年龄限制仅用于智能推荐，不会阻止手动选座后的预订或购票。

订单状态说明：

| 状态 | 可执行操作 |
|---|---|
| 已预订 | 可取消预订，座位恢复为空座。 |
| 已购票 | 可退票，座位恢复为空座。 |
| 已取消 | 历史记录，不可再次操作。 |
| 已退票 | 历史记录，不可再次操作。 |

普通用户只能查看和操作自己的订单。

已购票订单的“退票”按钮旁提供“评分”。点击后可选择 1–5 星，评分会保存在该订单中；已评分订单可点击“重新评分”修改。

## 4. 管理员后台操作

### 4.1 后台概览

管理员登录后可查看影厅数量、订单数量和用户数量。所有数据均来自当前浏览器的本地状态。

### 4.2 修改座位状态

1. 在“影厅与座位管理”选择小厅、中厅或大厅。
2. 在下拉框选择“空座”“已售”或“维修/禁用”。
3. 点击座位管理图中的目标座位。

修改会立即保存并同步到普通用户端。将有有效订单的座位改为空座或维修/禁用时，关联预订会取消、关联购票会退票，以保持座位与订单一致。

在手机端管理中厅或大厅时，画布会保留可读的座位尺寸。可直接在座位图上单指上下、左右滑动，或使用画布上方的方向按钮移动视图后再点击目标座位。

“重置当前影厅”会恢复该影厅的初始座位样例状态，并关闭该厅当前有效订单；请谨慎使用。

### 4.3 管理订单

管理员可查看全部订单的订单号、用户名、影厅、座位、状态和创建时间。

- 对“已预订”订单可点击“取消预订”。
- 对“已购票”订单可点击“退票”。

操作完成后，用户端座位图、订单状态和热度边框会同步更新。

### 4.4 查看普通用户

后台“普通用户”区显示普通用户的昵称、用户名、会员等级和注册时间。为保护演示数据，页面不会显示任何用户密码。

### 4.5 退出后台

点击页面顶部“退出登录”后，系统清除当前登录会话并返回登录页。

## 5. 常见问题

### 为什么刷新后座位和订单还在？

系统使用 LocalStorage 保存本地数据，因此刷新不会清空状态。

### 为什么某些座位不能点击？

已售、已预订和维修/禁用座位不可选择。普通用户还需要先登录才能操作座位。

### 为什么推荐结果和手动选择不一样？

推荐按照票型、年龄、连续座位与视角偏好自动生成；用户可在推荐后继续单选、按住 `Ctrl` 多选或拖拽调整。

### 如何彻底恢复初始数据？

清除浏览器中该站点的 LocalStorage 后刷新页面，系统会重新初始化管理员账号、影厅和样例座位数据。此操作会删除本浏览器中的所有本地订单和注册账号。
